{
  "name": "express-api",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "start": "pm2 start ecosystem.config.json --no-daemon",
    "dev": "cross-env NODE_ENV=development nodemon src/index.js",
    "test": "jest -i --colors --verbose --detectOpenHandles",
    "coverage": "jest -i --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prettier": "prettier --check **/*.js",
    "prettier:fix": "prettier --write **/*.js",
    "migration:generate": "cross-env NODE_ENV=development sequelize migration:generate --name",
    "seed:generate": "cross-env NODE_ENV=development sequelize seed:generate --name",
    "migrate": "cross-env NODE_ENV=development sequelize db:migrate",
    "migrate:undo": "cross-env NODE_ENV=development sequelize db:migrate:undo",
    "migrate:undo:all": "cross-env NODE_ENV=development sequelize db:migrate:undo:all",
    "seed": "cross-env NODE_ENV=development sequelize db:seed",
    "seed:all": "cross-env NODE_ENV=development sequelize db:seed:all",
    "seed:undo": "cross-env NODE_ENV=development sequelize db:seed:undo",
    "seed:undo:all": "cross-env NODE_ENV=development sequelize db:seed:undo:all",
    "importPharmacies": "cross-env NODE_ENV=development node src/scripts/importPharmacies.js",
    "importUsers": "cross-env NODE_ENV=development node src/scripts/importUsers.js"
  },
  "keywords": [],
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "etl": "^0.2.3",
    "express": "^4.17.1",
    "helmet": "^4.1.0",
    "http-status": "^1.4.0",
    "joi": "^17.3.0",
    "JSONStream": "^1.3.5",
    "morgan": "^1.9.1",
    "pg": "^8.8.0",
    "pm2": "^5.1.0",
    "sequelize": "^6.21.4",
    "swagger-jsdoc": "^6.0.8",
    "swagger-ui-express": "^4.1.6",
    "validator": "^13.0.0",
    "winston": "^3.2.1",
    "xss-clean": "^0.1.1"
  },
  "devDependencies": {
    "coveralls": "^3.0.7",
    "cross-env": "^7.0.3",
    "eslint": "^7.0.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-config-prettier": "^8.1.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-jest": "^24.0.1",
    "eslint-plugin-prettier": "^3.1.1",
    "eslint-plugin-security": "^1.4.0",
    "faker": "^5.1.0",
    "jest": "^26.0.1",
    "lint-staged": "^11.2.6",
    "node-mocks-http": "^1.8.0",
    "nodemon": "^2.0.0",
    "prettier": "^2.0.5",
    "sequelize-cli": "^6.4.1",
    "supertest": "^6.0.1"
  }
}
